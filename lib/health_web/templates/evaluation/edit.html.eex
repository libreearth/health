<h1>Edit Evaluation</h1>

<%= render "form.html", Map.put(assigns, :action, Routes.evaluation_path(@conn, :update, @evaluation)) %>

<span><%= link "Back", to: Routes.evaluation_path(@conn, :index) %></span>
